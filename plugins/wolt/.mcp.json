{
  "mcpServers": {
    "wolt": {
      "command": "node",
      "args": [
        "--input-type=module",
        "--eval",
        "import { resolve } from \"node:path\"; import { pathToFileURL } from \"node:url\"; const root = process.env.CLAUDE_PLUGIN_ROOT || process.cwd(); await import(pathToFileURL(resolve(root, \"dist/server.js\")).href);"
      ],
      "cwd": "."
    }
  }
}
